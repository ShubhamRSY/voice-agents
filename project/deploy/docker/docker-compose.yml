volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:

networks:
  nexus:
    driver: bridge

services:
  # ── TLS Termination ──────────────────────────────────────────
  # Caddy automatically provisions Let's Encrypt certificates.
  # Set DOMAIN in .env for production, or use ":80" for local dev.
  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      DOMAIN: ${DOMAIN:-:80}
      NEXUS_URL: http://nexus:8000
    networks:
      - nexus
    depends_on:
      nexus:
        condition: service_healthy
    restart: unless-stopped

  # ── Database ─────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env — openssl rand -hex 32}
      POSTGRES_DB: nexus
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexus"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nexus
    restart: unless-stopped

  # ── Cache & Task Queue ───────────────────────────────────────
  redis:
    image: redis:7-alpine
    command: ["sh", "-c", "redis-server --requirepass \"$$REDIS_PASSWORD\""]
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env — openssl rand -hex 32}
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "sh", "-c", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nexus
    restart: unless-stopped

  # ── Application ──────────────────────────────────────────────
  nexus:
    image: ghcr.io/shubhamrsy/nexus-ai:latest
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    expose:
      - "8000"
    env_file:
      - ../../config/environment/.env
    volumes:
      - ../../data:/app/data
      - ../../config:/app/config
    environment:
      DATABASE_URL: postgresql://nexus:${POSTGRES_PASSWORD}@postgres:5432/nexus
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      CORS_ORIGINS: ${CORS_ORIGINS:?Set CORS_ORIGINS to your HTTPS origin(s), comma-separated}
      TWILIO_WEBHOOK_BASE_URL: ${TWILIO_WEBHOOK_BASE_URL:-https://${DOMAIN}}
      APP_ENV: production
      AUTH_REQUIRED: "true"
      DEMO_MODE: "false"
      ALLOW_REGISTRATION: "true"
      # GUNICORN_WORKERS: set in config/environment/.env (compose `environment` overrides env_file)
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    networks:
      - nexus
    restart: unless-stopped

  # ── Automated Backup ────────────────────────────────────────
  backup:
    image: alpine:3.19
    volumes:
      - ../../data:/app/data
      - ../../config:/app/config
      - ../../backups:/app/backups
      - ../../scripts:/app/scripts:ro
    environment:
      BACKUP_DIR: /app/backups
      CHROMA_PERSIST_DIR: /app/data/chroma
      DATABASE_URL: postgresql://nexus:${POSTGRES_PASSWORD}@postgres:5432/nexus
      BACKUP_RETENTION_DAYS: 30
    entrypoint: |
      sh -c "
        apk add --no-cache postgresql-client aws-cli >/dev/null 2>&1
        echo 'Backup container ready — waiting for schedule...'
        echo '${BACKUP_CRON:-0 3 * * *} /app/scripts/backup.sh >> /app/backups/backup.log 2>&1' > /tmp/crontab
        crontab /tmp/crontab
        crond -f
      "
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - backup
    networks:
      - nexus
    restart: unless-stopped
